bot.log
bot.send
.env
bot.startup
bot.stdout.log
aup_watchdog.log
roll_counter.log
rolls.json
rolls.json.bak-*
scrub.log
__pycache__/
*.pyc

# Raw source PNGs from Gemini — only compressed JPEGs under docs/img/ are committed
docs/img/*.png

# Private — channel-specific memory, not for public repo
memory/

# Host-specific Claude Code settings (paths, WebFetch domains)
.claude/settings.local.json

# Reference material (private copies of Azzurra helpers docs, etc.)
refs/

# Scratch / transient workspace crud (trim dumps, cleanup tsvs, runtime locks)
*.lock
activity_log_trim_dump.txt
insulti_*.tsv
insulti_*.txt

